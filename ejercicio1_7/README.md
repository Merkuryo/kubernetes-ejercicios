# Log Output with HTTP Endpoint and Ingress

Esta versión del Log Output incluye:
- Endpoint HTTP que devuelve timestamp y random string
- Logging automático cada 5 segundos
- Ingress para acceso externo
- Service tipo ClusterIP

## Development
```bash
npm install
npm start