# Todo Application

Simple web server that displays the port it's running on.

## Requirements
- Node.js
- Docker
- Kubernetes (k3d)

## Development

```bash
# Install dependencies
npm install

# Start server (default port 3000)
npm start

# Start with custom port
PORT=8080 npm start