apiVersion: v1
kind: Service
metadata:
  name: wikipedia-svc
  namespace: default
  labels:
    app: wikipedia
spec:
  type: NodePort
  selector:
    app: wikipedia
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
