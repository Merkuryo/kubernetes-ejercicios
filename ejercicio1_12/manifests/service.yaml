apiVersion: v1
kind: Service
metadata:
  name: project-svc
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30002
      protocol: TCP
  selector:
    app: project
