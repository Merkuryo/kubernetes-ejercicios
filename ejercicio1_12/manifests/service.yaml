apiVersion: v1
kind: Service
metadata:
  namespace: project
  name: project-svc
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30005
      protocol: TCP
  selector:
    app: project
