apiVersion: v1
kind: Service
metadata:
  name: todos-db-svc
  namespace: project
  labels:
    app: todos-db
spec:
  ports:
  - port: 5432
    name: postgres
  clusterIP: None
  selector:
    app: todos-db
